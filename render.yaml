services:
  - type: web
    name: qdii-fund-dashboard
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node --experimental-sqlite backend/api/server.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_VERSION
        value: 22.12.0
