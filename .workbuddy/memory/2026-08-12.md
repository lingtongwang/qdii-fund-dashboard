# 2026-08-12 — QDII Fund Dashboard：评级移除 + PDF 数据源 + 部署健壮性

## 今日决策（用户拍板）
1. **评级字段彻底删除**：前端（script.js / index.html）不再展示评级；PRD §4.2 删除 `ratings` 字段；后端不再输出。原依赖的 `jjpj` 端点本就停用，因字段取消无关紧要。
2. **行业/区域/资产配置 → 季报 PDF 抽取**：用户明确要求从季报 PDF 取这 3 个字段。
3. **部署健壮性最大化**：节流+单基金隔离+重试、TZ=Asia/Shanghai、node:sqlite 免原生编译、NAV 历史深度、PDF 并发控制、失败沿用、进程守护。

## 真机探活结论（curl，2026-08-11/12）
- **东财已死**：`ggmx`（公告ID，含 ut 令牌）→ `{"ErrCode":4,"ErrMsg":"404"}`；`FundArchivesDatas.aspx` 的 `hytz`/`qscc`/`zcpz`/`jjpj` 全部 12B 空壳。
- **东财仍活**：`lsjz`(净值/状态/数据日期)、`jjcc`(持仓)、`jbgk`(概况HTML,GBK)、`jdzf`(业绩排名)。`fundgz`(估值)返回 HTML 页不稳。
- **季报 PDF 实际可由东财干净获取（2026-08-12 晚修正，推翻上面 eid 唯一源结论）**：`np-cnotice-fund.eastmoney.com/api/content/ann?art_code=AN{timestamp}{seq}`（HTTP、免费、无 token、`access-control-allow-origin:*`）返回 `attach_url`→`https://pdf.dfcfw.com/pdf/H2_AN{...}_1.pdf`（实测有效 PDF v1.5、18 页、560KB）+ `notice_content` 季报全文文本；三张配置表（行业/区域/资产）在文本中结构化存在，正则即可抽取，**无需解析 PDF**（PDF 仅兜底）。**Playwright 不再用于下载/解析 PDF。**
- **唯一待解：art_code（公告 ID）发现**：东财未暴露干净列表端点（实测 `ggmx` / `np-cnotice-fund/api/notice/list` / `datacenter` 多个 `reportName` 全 404；`fund.eastmoney.com/gonggao/CODE.html` 为 404 空壳页）→ 用证监会 `eid.csrc.gov.cn`（Playwright 过 JS 网关，季更、结果可缓存）取得 art_code，该 ID 与东财 `art_code` 同源同格式，可直接喂给上面内容接口。Playwright 仅用于"发现 ID"这一步（轻量、低频），不再碰 PDF。
- **node:sqlite 在托管 Node 22.22.2 可用**（加 `--experimental-sqlite`），零原生编译，部署最稳。

## 改动文件
- `script.js`：删除评级归一化/对比页/详情页显示 + mock 数据 ratings 字段（语法通过，grep 无残留）。
- `index.html`：删除 `rating-box`（rt-wind/rt-star）展示块。
- `BACKEND_DESIGN_AND_PLAN.md`：重写为 **v5**（删评级；3 字段改 eid PDF + Playwright；§九 部署健壮性清单；node:sqlite / TZ / 节流隔离重试 / NAV深度 / PDF并发 / 失败沿用 / pm2）。
- `QDII_Fund_Dashboard_Comprehensive_PRD.md`：§4.2 删 ratings 字段；mock 表 1-3 行 hot→other/us（与前端一致）；详情/对比展示删评级行；状态表与脚注同步。

## 待办
- 后端 `backend/` 尚未建；按 v5 阶段 0–3（约 3.5 工作日）实施。Playwright 需在部署机 `npx playwright install chromium`（本环境未实跑完整浏览器流程，属构建阶段落地）。
- 11 个 `_probe_*.js` 探针脚本仍散落工作区，开工前建议归档/删除。

## 2026-08-12 晚 · 用户指出"东财明明有季报 PDF"后真机复核（重大修正）
- **用户贴出** `fund.eastmoney.com/gonggao/270042,AN202607201827132608.html` 质疑"东财找不到季报 PDF"的旧结论。复核证明：东财**确有**季报 PDF，且比想象中干净。
- **关键接口**：从详情页加载的 `gonggao_content_min_*.js` 找到内容接口 `np-cnotice-fund.eastmoney.com/api/content/ann?art_code=AN...`，返回 PDF 直链 + `notice_content` 全文。270042 是 ETF 联接基金，季报写"未持有股票"，属正常（抽取需识别"未持有"留空）。
- **已修正文档**：`BACKEND_DESIGN_AND_PLAN.md`（§0 数据来源表、§2.2 死端表、§2.3 重写、字段审计表、架构图、调度、依赖、阶段1、§八.7、版本说明）+ `QDII_Fund_Dashboard_Comprehensive_PRD.md` §4.2 字段语义约定。核心变化：**季报来源从"证监会 eid + Playwright 抓 PDF"改为"东财 np-cnotice-fund 内容接口（HTTP）+ 文本抽取"；Playwright 仅可能用于发现 art_code**。
- **仍待解（构建阶段）**：art_code 发现机制（东财无干净列表接口）。可选项：① 证监会 eid（Playwright，季更+缓存）；② 部署阶段继续找东财公告列表端点；③ 15 只基金一次性人工捕获 + 季更刷新。
