# 2026-08-16 工作日志

## report_resume.mjs 自驱动运行 + 本机 IP 被东财长期封禁

- 用户本机 `nohup node report_resume.mjs &`（PID 56105）已自驱动运行，无需逐次手敲。
- 进度（截至本 turn）：done=46 / none=37 / fail=1 / 剩余≈655。region_alloc 从 13 涨到约 46 只，远未补齐（目标 739）。
- 阻塞根因：**用户 MBA 公网 IP 被东财对 `np-cnotice-fund` 长期封禁**（非瞬时抖动），脚本反复撞预检失败，旧逻辑以 15min 规律退避空转（可能拖长封禁）。
- 优化：把限流退避改成「深度休眠」——`throttleStreak<=5` 指数退避到 15min 封顶；之后间隔随封禁时长拉长（15→20→…封顶 60min）并 ±10min 随机抖动；**永不主动退出**（IP 恢复即自动续跑）；第 30 次打印换 IP 建议。已写入 report_resume.mjs。

## 硬约束（重要，须对用户透明）
- 季报正文接口 `np-cnotice-fund.../api/content/ann` **只有用户本机直连公网的 IP 能访问**；沙箱出口经代理 198.18.0.62，该 IP 被东财封死（UND_ERR_SOCKET），**无法从沙箱代跑抓取**。
- 因此"启动抓取"这一步物理上只能从用户电脑发起。用户已启动的进程（PID 56105）不会退出，会在本机 IP 解封后自动续跑完。
- 零手动出路：①等东财解封（数小时）旧进程自愈；②把 MBA 切手机热点/VPN 换 IP，旧进程下一轮预检通过即自动续跑。两者都不需用户敲命令。

## 待办
- 用户报「全部完成 ✅」后，从沙箱核 `region_alloc` 最终覆盖（SELECT COUNT(DISTINCT code)），确认无漏网。

## 深度研究：绕开东财封禁的替代季报源（实测 2026-08-16）

东财 `np-cnotice-fund` 封的是"我们的 IP"，根本解法是**换独立数据源**。实测 5 条路线：

1. **晨星中国 morningstar.cn ✅ 最推荐（沙箱可代跑）**
   - `https://www.morningstar.cn/fund/{code}.html` 含「股票地区分布」表格，**SSR 干净 HTML**（Vue 但数据在源码），`fundCode` 直抓、**无需映射**、**域名独立、沙箱代理未被封**（实测 000906/513100/270042 均 200 且解析成功，14 行地区分布）。
   - 解析：`<td class="col-name"[^>]*>([^<]+)</td><td[^>]*>([\d.]+)</td>`（首个数值=投资组合%）。
   - 口径：晨星**洲/地区级**（大亚洲地区/日本/北美/大欧洲地区/英国…），非东财"国家（地区）证券市场"级。合计约 200%（大洲汇总+国家明细）。
   - 价值：可立刻从沙箱批量抓 739 只补齐 `region_alloc`，**完全绕开东财、用户零操作**。

2. **证监会 eid.csrc.gov.cn（最权威，需 fundId 映射）**
   - `instance_show_pdf_id.do?instanceid=XXX` PDF **可达**（679KB 真 PDF，域名独立）。
   - `fund_detail.do?fundId=N` 含全部季报 PDF 链接 + XBRL 接口 `list_net_classification.do?reportType=FB040&fundCode=CODE&classification=2030-10x0`（用 fundCode）。
   - 卡点：无公开"代码→fundId"搜索接口（猜测全 404）；QDII 的 XBRL 分类元素 ID 与 A 股不同。批量需先建 code→fundId 映射（工程大）。

3. **cninfo**：PDF 托管可达，但 `hisAnnouncement/query` 对基金搜索不精确（中文名 0 条、代码模糊 621 条不相关），不适合批量。

4. **基金公司官网 PDF**（广发 gffunds/富国 fullgoal/摩根 cninfo）：最权威、各域名独立，但需逐家适配（管理人集中可硬编码）。

5. **东财原接口**：本机 IP 被封，用户进程（PID 56105）深度休眠续跑中。

### 口径权衡（关键决策点）
- 东财/eid = **国家（地区）证券市场级**（细：美国/中国香港/日本/韩国…）
- 晨星 = **洲/地区级**（粗：北美/大欧洲/亚洲…）
- 混存 region_alloc 会导致 dashboard「区域」语义不一致。需用户拍板：统一晨星口径（立刻全量）vs 等东财国家级（更细但受封）。

### ✅ 执行结果（用户选晨星口径）
- 写了 `report_morningstar.mjs`（沙箱代跑，晨星域名独立、代理可达），已停掉本机东财 `report_resume`(PID 56105) 避免锁冲突。
- 4 批跑完：done=570 / none=169 / fail=0 / 剩余=0。`region_alloc` 1296 行，覆盖 570 只（report_date='morningstar'）。
- 解析取 `<tr class="row-head">` 大洲级汇总行（合计≈100%），DELETE 旧行后 INSERT，统一晨星口径（清掉东财国家级旧数据）。
- 样例 000906：美洲 69.4% / 大亚洲 25.0% / 大欧洲 5.6%。
- 169 只 none = 晨星无地区分布（债券/原油类 QDII 或晨星无该基金）。
- **结论：区域配置已绕开东财封禁全量补齐，用户零操作。** 行业配置(industry_alloc, HYPZ, 463只) 未受影响（HYPZ 接口未被封）。

## 后端完成度体检（2026-08-16 晚 23:00）

实际跑库诊断（distinct code / 739）。结论：**采集+入库+API+前后端对接主体已完成**；除天然无数据的维度外覆盖率 97-100%。

覆盖率：funds 100% / daily_nav 98.2%(726,缺13) / risk 97% / asset 97.2% / perf 98.2% / region 77.1%(570) / industry 62.7%(463) / holdings 70.9%(524) / limit 处理100%(282有限购+444无限购NULL) / quarterly_ann 8.8%(65)。

伪缺口（非真缺）：
- limit 38.2% 是"有限购占比"，NULL=无限购，设计正确。
- region 77%/industry 63% 大部分合理：债券/商品无"地区股票分布"、指数基金无主动行业配置。

真值得优化：
1. 13 只缺净值：4 后端份额+2 美元+3 外币(东财无数据,合理)；4 只 027xxx 新发(交银港股消费027450/027451、华宝医药精选027755/027756)疑似真遗漏→补抓。
2. 地区缺 22 只混合偏股、行业缺 16 只混合偏股(主动股基应有数据,可能漏抓)→针对性补。
3. scheduler 已注册 cron(每日18:30净值/季10号全量)但无守护进程(pm2/systemd)→重启即停。
4. report_resume.mjs(东财)残留:误跑会污染 region_alloc(晨星口径已统一)→加 source 列防护或清理。
5. quarterly_ann 仅 65 只,现几乎不被消费→确认前端是否用,否则清理。
6. 无数据质量监控/告警(health 有覆盖率但无"净值停更/覆盖率降"告警)。

前端对接已验证:只调 /api/funds 拿全量,gatherFund 聚合 24 字段(industry/market 为 {labels,data} 结构),详情 openSheet 从内存取,渲染容错完整。

## 优化执行（用户勾选全部 4 项，2026-08-16 晚）

- #29 补混合偏股地区/行业：实测 22 只缺 region + 16 只缺 industry 全为**源无数据**（晨星 progress=none / HYPZ progress=skip，新发基金未公布持仓/地区/行业），无法补，系真实上限非 bug。
- #30 补净值：13 只特殊份额实测东财 pingzhongdata + lsjz 均空（lsjz 返回 ErrCode:0 但 LSJZList=[] TotalCount=0），含 4 只新发(027450/027451/027755/027756)，源无数据无法补。nav 98.2% 即真实上限。
- #31 scheduler 守护：新增 backend/jobs/ecosystem.config.cjs（pm2：server+scheduler 两进程，autorestart+min_uptime，logs/ 目录）。验证 scheduler cron 注册成功、server listen:3000 成功。本机启动：`pm2 start backend/jobs/ecosystem.config.cjs && pm2 save && pm2 startup`。
- #32 清理+告警：
  - region_alloc 加 source 列（db/sqlite.js initSchema ALTER+回填），1316 行全标 morningstar，0 NULL。
  - orchestrator.ingestReport **移除 region_alloc 写入**（防东财国家/地区级口径污染已统一的晨星口径）。
  - scheduler 季度任务改 withReport:false（避被封的 np-cnotice 接口 + 不污染 region/industry）。
  - 新增 backend/jobs/monitor.mjs（覆盖率+净值停更告警，挂 scheduler 每日/季度任务后）。
  - report_resume.mjs 加弃用注释（region 改 report_morningstar 维护）。
  - server.js /api/health 的 q 取列名 bug 同修（Object.values(...)[0]，原 ['count(*)'] 在 node:sqlite 下返回 undefined）。
- 最终覆盖率：funds100 / dailyNav98.2 / risk97 / asset97.2 / perf98.2 / region77.1 / industry62.7 / holdings70.9 / limit100%(处理)。核心层均≥97%，monitor ✅。

## 后端分类体检（2026-08-16 末）

- 全 739 只已分类，100% 覆盖，5 维度：`tab`(地理大区 us/apac/europe/other) + `pill`(子标签) + `raw_type`(东财产品种类) + `risk_level` + `classify_basis`(命中规则)。
- 分类实现 `backend/classify.js`：`classify(name,type)` 仅按**基金名+东财raw_type 关键词硬匹配**，不吃真实持仓数据。这是当初无 region 源时的设计。
- 质量问题：tab='other' 297 只(40.2%)、无匹配兜底 163 只(22%)，均为"名字猜不出地区"的兜底。
- 改进机会：现已有 `region_alloc`(晨星股票地区分布, 570 只)。其中 153 只 other 基金**已有真实地区数据**，148 只可用真实持仓反推出具体地区（美洲→us / 欧洲→europe / 亚洲·日本→apac），仅 5 只真全球分散仍 other。即 tab='other' 可从 297 降到约 149（减半）。待用户拍板是否升级分类为"持仓数据驱动"。

## 基金筛选：标记债券/商品/REITs 为"暂时不用"（2026-08-16 末）

- 需求：QDII 太多，先只保留股票为主的基金；债券/商品/REITs 三类暂时不用；**不删数据，只标记**。
- 实施：`funds` 表加 `excluded INTEGER DEFAULT 0` 列（sqlite.js 建表+ALTER 双保险）。
- 新增 `backend/jobs/mark_excluded.mjs`（幂等）：把 pill IN ('QDII债券','QDII商品','QDII REITs') 标 excluded=1；`--reset` 可全量恢复。
- 读取路径：`gatherAll({includeExcluded})` 默认过滤 excluded=1；`/api/funds` 默认只返保留基金，`?all=1` 临时看全量；`/api/health` 新增 funds:{total,active,excluded}。
- 验证：标记 125 只（债券83+商品29+REITs13），保留 614 只；/api/funds=614、?all=1=739、health={total:739,active:614,excluded:125}，默认列表 0 漏出。名单由 classify.js 的 pill 决定，与展示一致。数据零删除，scheduler 仍刷新全 739 只。
