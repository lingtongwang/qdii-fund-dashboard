# 2026-08-13 — 开工前最后一次前后端逻辑质检

对前端三件套（script.js/index.html/style.css）+ 两份文档（BACKEND_DESIGN_AND_PLAN.md / PRD）做了完整交叉核对，发现并修复了一批「契约错位 / 崩溃路径 / 文档矛盾」，现已全部改完。

## 已修复（前端代码）
- `fmtPct(null)` 原返回 "0.00%"，改为返回「—」（列表/对比/业绩的 null 指标不再误显示为 0）。
- `openFullDetail` 直接读 `currentFund.assets.labels` 未判空 → 被动/联接基金 assets=null 时整页崩溃；改为判空 + 显示「不适用」并隐藏图表容器（id=asset-chart-wrap）。
- `safeRenderChart` 调用处 `currentFund.market.labels` 是**急切求值**，market=null 时还没进守卫就抛错；openSheet/openFullDetail 改为 `currentFund.market || {labels:[],data:[]}` 先兜底。
- `generateCompare` 的 `unionLabels`/`buildDatasets` 直接 `f[field].labels` 未判空 → 任一对比基金 market/industry=null 崩溃；已加判空。
- 按限额穿透 `w = f.limit`：后端 `limit:null`（无限额）时 totalWeight=NaN → 穿透报告全错；改为 null 按等权基线 1 处理。
- 排序比较器 `valB-valA` 遇 null 得 NaN；改为 null→±Infinity。
- `perf.r` 为 null 时 `returnStr=p.r` 渲染出字符串 "null"；改为「—」。
- `renderHoldingsList` 的 `h.r.toFixed(2)` 加 `?? 0` 兜底。
- 新增 `purchaseStatus` + `dataAsOf` 字段并接入底部抽屉与深度研报 UI（PRD 要求"UI 须标注数据日期"，此前完全缺失；原"当前申购状态"卡片实际显示限额，已拆成「申购状态」+「单日限额」两张卡）。
- 删除已废弃的 `id` 字段（makeFund）与 CSS 中残留的 `.rating-*` 死规则（评级已移除）。

## 已修复（文档契约）
- BACKEND §4.6 补 `riskMetrics` mapper 映射：表列 `max_drawdown`→`drawdown`、`volatility`→`vol`（前端按 drawdown/vol 读取，否则回撤/波动率上线后静默显示「—」）；并新增**比例单位硬约束**：alloc 表存小数(0.4521)，mapper 必须 ×100 成百分比(45.21)，否则前端显示 0.45%；holdings.pct 同理。
- PRD §4.1 去掉错误的 `tracking_index` 分类依据（后端实际用 benchmark/target/scope）。
- PRD §6.1/§8/§5.4.4/归一化步骤 把数字 `id` 描述统一为 `code`（字符串）；§9 "5个大区"→4；§10.4 缓存版本 v10→v15；§4.3 表头 "ID"→"序号"。

## 仍未完全闭环 / 待决策（未自动改）
- 季报 art_code（公告 ID）发现环节东财无干净列表接口，仍需证监会 eid/Playwright 或解析公告页——文档已标注为唯一未闭环链路。
- `nav`/`accNav` 在模型里但前端未展示（仅 dataAsOf 要求展示，已加）。
- 旧 localStorage 里若存过数字 id 的 fav_funds，升级后会被新 code 过滤逻辑静默丢弃（首次加载清空），建议加一次性迁移或提示。
- 11 个 `_probe_*.js` 探针脚本仍散落工作区，建 backend 前建议归档/删除。
- `industry` 在深度研报页未单独展示（仅对比/穿透报告出现），属产品设计，非 bug。

---

# 2026-08-13（续）— 用户拍板 5 项优化修改，已全部落地

用户就质检遗留 6 项拍板：art_code 发现改"解析公告页"、nav/accNav 上 UI、旧 localStorage 迁移不管（测试数据）、删 _probe_*.js、industry 在研报页单独展示。

## art_code 发现环节（真机验证后定稿，纯 HTML 解析、零浏览器依赖）
- 实测：东财 `ggmx`（各种 ut 令牌）/ `fundf10` jjgg 列表 / `np-cnotice` list / `datacenter` 多 reportName **全部 404**；`fund.eastmoney.com/gonggao/CODE.html` 是 404 空壳。
- **可用源：`fund.eastmoney.com/{code}.html`（基金主页，121KB 服务端渲染）含最新 ~5 条 `gonggao/{code},AN{id}.html` 链接（截断标题 newsTit + 日期 newsData）**；内容接口 `np-cnotice-fund.eastmoney.com/api/content/ann?art_code=AN...` 返回**完整标题** notice_title（如"…联接基金(QDII)2026年第2季度报告"）+ notice_date + PDF 直链 + notice_content 全文。
- 发现流程：cheerio 解析主页→取最新 5 条 art_code→逐条调内容接口取完整标题→过滤"季度报告"取最新者→art_code 缓存进 DB（PDF 永久有效，跨周期沿用）。边界：主页仅 5 条，季报被挤出时靠缓存/季更复扫/初始人工播种兜底。
- 文档 BACKEND_DESIGN_AND_PLAN.md 已全面改写：§2.3 发现环节、数据来源表、§八.7、依赖清单（删 playwright/eid）、版本说明——**整套流程零浏览器依赖**。

## 前端 UI 改动（script.js / index.html / style.css）
- `makeFund` 新增 `nav`/`accNav`（带确定性默认值，便于 mock 演示；后端用真实值覆盖）；新增 `fmtNav`（4 位小数 / 空值「—」）。
- 底部抽屉新增"单位净值 / 累计净值"卡片；深度研报页基础卡片扩为 4 框（单日限额/申购状态/单位净值/累计净值）；新增"行业分布"独立板块（横向柱状图 industryChartFull + 行业 pill 文本，含 null 判空显示「不适用」）。
- `node --check` 通过。

## 其他
- 已删除 11 个 `_probe_*.js`（用户要求，免混淆）。
- 旧 localStorage 迁移：用户明确不管（均为测试数据），未改。
- 后端 §4.6 已映射 `nav`/`accNav` 为输出字段，与前端读取一致，无矛盾。

---

# 2026-08-13（续2）— 第二轮全面自检（文档 + 前端，为后端动工准备）

用户要求"再来一轮全面自检，保证没有问题，为后端开发准备好"。在上一轮基础上又扫出并修复 4 处契约/健壮性错位：

## 修复（前端）
- 删除 15 条 mock 数据里残留的 `id: N` 死字段（makeFund 从不读 `o.id`，与 PRD"数字 id 废弃、统一用 code 作主键"矛盾）；现 mock 仅以 `code` 为主键，与后端契约一致。
- 无限额基金的 `limit` 由历史哨兵 `99999999` 改为 `null`（对齐后端 §4.6 `null`=无限额 契约）；前端 `formatLimit`/`numOrInf` 已兼容 null，且穿透权重 `w=(f.limit==null?1:f.limit)` 的注释"等权基线处理"现在才真正成立。
- `renderChart` 单数据集（环形/单组柱）调色板不足时自动补齐（按 CHART_COLORS 循环），避免真实数据 `region_alloc` 国家数 > 调色板长度时出现灰色缺色——纯健壮性，mock 不受影响。

## 修复（文档）
- PRD §5.4.1 / §5.4.2 / §交互：`fav_funds` 值由"基金 ID 数组"改为"基金 code（6 位字符串）数组"；`toggleFavorite(id)`→`toggleFavorite(code)`（与前端实现一致）。

## 交叉核对结论（后端可直接动工）
- 后端字段契约 §4.6 与前端 `makeFund` 输出逐一对齐：scale/fee/risk 字符串、nav/accNav 数值、holdings→{n,r}、market/industry/assets→{labels,data}(百分比数值)、riskMetrics 键映射(max_drawdown→drawdown, volatility→vol)、ratings 不输出。
- 文档无 playwright/eid/wind.js/tracking_index/hot(仅"已移除")/ggmx 等矛盾引用；季报源统一为 np-cnotice-fund + 基金主页 HTML 解析 art_code。
- 仍为"设计决策、非 bug"项留给后端：按限额穿透时无限额基金(null→等权基线 1)是否要特殊处理；区域分布超过调色板时前端已自动补齐但建议后端聚合 top-N+"其他"。

---

# 2026-08-13（续3）— 用户新增两条业务规则，已落地前端 + 文档

用户要求：① 选中无限额基金且选择"按限额穿透"时，直接弹"无限额基金无法穿透"；② 饼图切片控制在 15 块内，超出部分按占比最小合并为"其他"。

## 前端改动（script.js）
- `generateLookthroughReport()` 开头新增守卫：当 `weightMode==='limit'` 且任一已收藏基金 `f.limit==null` 时，`alert('无限额基金无法穿透')` 并 `return` 中止生成报告（不再用之前的"无限额→等权基线 1"逻辑）。权重计算 `w=f.limit` 现保证为数值（无 NaN）。
- 新增 `capSlices(labels, data, maxSlices=15, otherLabel='其他')`：分类数 >15 时保留占比最大的 14 项 + 合并剩余为「其他」（已有的"其他"先拎出避免重复 label）。在 `renderChart` 的 `doughnut` 分支自动应用，所有饼图统一 ≤15 瓣；Bar（区域/行业）不受影响。
- `node --check` 通过。

## 文档改动（PRD §5.4.4）
- 新增"约束（业务规则）"小节：明确①按限额穿透拦截规则、②饼图切片上限 15+「其他」合并规则（renderChart 已实现）。

## 说明
- 此前续2 里"无限额→等权基线 1"的设计决策被本规则取代；区域分布 top-N+其他 的"建议"现由前端饼图自动兜底（仅限 doughnut）。

---

# 2026-08-13（续4）— 第三轮全面自检（冲突 + 隐患），修一处真实 bug

用户要求"再检查一遍文档和前端代码，看看有没有冲突和存在的隐患"。重读 script.js 全文 + 交叉核对 index.html 的 DOM id 与 BACKEND_DESIGN_AND_PLAN.md §4.6 字段契约。

## 已修复（前端，真实 bug）
- `renderChart` 图例逻辑：原 `isBar` 分支硬关 `legend.display=false`，导致**多基金对比图（region/industry，isBar=true 且带 extraDatasets 多序列）图例被关闭、无法区分各基金颜色**。改为：有 extraDatasets（多序列）一律显示图例；仅单序列柱状图（穿透报告区域/行业，靠 y 轴标签）才关闭图例；doughnut 单序列沿用默认图例。
- 穿透报告资产配置饼图调色板由 3 色硬编码改为 `CHART_COLORS`，避免切片 >3 时按 7 色循环补齐出现"前 3 自定义色 + 后几色风格不一"的不一致（capSlices 上限 15 后更易触发补齐）。

## 交叉核对结论（无冲突）
- index.html 全部 JS 引用 DOM id（含上轮新增 sheet/full 的 nav/accnav/purchase/dataAsof、industry-chart-wrap、asset-chart-wrap、report-fund-count、compare-fund-count 等）均存在，无孤儿引用。
- BACKEND_DESIGN §4.6 字段契约与前端读取逐一对齐：`limit`→null=无限额、`purchaseStatus`/`nav`/`accNav`/`dataAsOf` 由 daily_nav 注入、`riskMetrics` 键映射 max_drawdown→drawdown / volatility→vol、`ratings` 已删除。文档与代码无矛盾。

## 隐患/提醒（非 bug，已说明，未改）
- `capSlices` 的"已有其他"去重只认精确字符串「其他」；若后端/数据源用「其它」「其他资产」等变体，会生成并列两个"其他类"切片。建议后端统一残余分类名为「其他」。
- 按限额穿透拦截发生在"生成报告"运行时（而非切换 radio 时），与"选择按限额穿透"语义一致；提示仅弹「无限额基金无法穿透」，未列具体基金名——如需更友好可后续加基金名。
- 守卫为"任一选中的无限额基金即整体拦截"，符合用户字面要求；多选含一只无限额基金时整份报告不生成，需用户自行取消该基金收藏。
