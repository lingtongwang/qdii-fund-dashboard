# 2026-08-17 · fund_dashboard status check

Reviewed whether frontend/backend are "done." Status: developed & works end-to-end when started, but NOT deployed/running.

## Verified facts
- API smoke test (`PORT=3999 node --experimental-sqlite backend/api/server.js`) works: `/api/funds` returns 119 visible (mainstream) funds with real data; `/api/health` ok.
- DB `backend/data/fund.db` (~41MB) populated: 739 funds total, 323506 daily_nav, 4932 holdings, 717 risk_metrics, 2890 industry_alloc, 1316 region_alloc, 2154 asset_alloc, 71 quarterly_ann. Visible (mainstream & !region_excluded & !excluded) = 119 → matches exports/主流基金_119只.xlsx.
- Frontend (index.html/script.js/style.css) wired to `fetch('/api/funds')` with 15-fund mock fallback. Served by backend static hosting.

## Key architectural divergences from BACKEND_DESIGN_AND_PLAN.md (doc is OUTDATED)
- Scope grew from 15 mock funds → full 739-fund QDII universe (backend/config/funds.js auto-generated 2026-08-14).
- Main source is `pingzhongdata` (adapters/eastmoney.js), NOT lsjz/jjcc/jdzf/jbgk as doc says.
- `np-cnotice` full-text interface (doc §2.3) is BLOCKED → industry_alloc filled by `report_industry.mjs` (eastmoney HYPZ endpoint), region_alloc by `report_morningstar.mjs` (morningstar.cn). These are separate one-off scripts NOT triggered by quarterly cron (which runs withReport:false).
- asset_alloc IS filled by main ingest (pingzhongdata).

## What remains (not done)
1. No persistent process: server + scheduler must be started manually; no pm2/systemd/Nginx. DB NAV latest = 2026-08-13 (stale).
2. Quarterly industry/region refresh is manual/fragile.
3. No README/run-book; no test suite in package.json.

User (lingtongwang) communicates in Chinese. Project is Chinese-language.
